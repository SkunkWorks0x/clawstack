{
  "name": "clawstack",
  "version": "0.1.0",
  "private": true,
  "description": "The Operating System for OpenClaw Agents — Security, Cost, Memory, Orchestration, Deployment",
  "author": "Imani (@Skunkworks0x)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Skunkworks0x/clawstack"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "clean": "rm -rf packages/*/dist",
    "dev:dashboard": "npm run dev --workspace=packages/dashboard"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
